// Select the static HTML theme used by Shiroa.
//
// Supported values are `starlight` and `mdbook`. Keep this setting in one
// place so theme experiments do not require editing the page template.
#let web-theme = "mdbook"
